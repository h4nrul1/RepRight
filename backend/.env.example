# Copy this file to .env and fill in your values

# PostgreSQL (AWS RDS) — paste your RDS endpoint here
DATABASE_URL=postgresql://postgres:yourpassword@your-rds-endpoint.rds.amazonaws.com:5432/postgres

# Leave blank to use local SQLite during development (no RDS needed)
# DATABASE_URL=sqlite:///./reprightdb.sqlite
